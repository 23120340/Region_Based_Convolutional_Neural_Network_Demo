[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pen-assembly-mvp"
version = "0.1.0"
description = "Config-driven MVP for monitoring click-pen assembly"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

