[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hybrid-assembly-monitor"
version = "0.1.0"
description = "Reusable YOLO and ViT-BiLSTM framework for assembly monitoring"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
